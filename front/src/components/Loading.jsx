import style from '../styles/Loading.module.css';

export default function Loading(props){
    return <div className={`${style.logo} ${style.border}`}>

                <div className={`${style.logo} ${style.borderTwo}`}>
                    <div className={`${style.logo} ${style.borderCenter}`}>

                    </div>


                </div>

            </div>
   
}